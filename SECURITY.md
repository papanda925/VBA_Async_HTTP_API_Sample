# セキュリティ方針

## 教材としての利用範囲

このリポジトリの簡易HTTPサーバーは、Windows版Excel VBAで通信の仕組みを学ぶためのものです。認証、認可、TLS、アクセスログ保護、同時実行制御、攻撃検知など、公開Webサーバーに必要な機能は備えていません。

次の用途には使用しないでください。

- インターネットや社内LANへ公開する
- 個人情報、認証情報、機密情報を送る
- 業務システムのAPIサーバーとして常時稼働させる
- 信頼できない相手から受け取った要求を処理する
- マクロ実行を許可していない組織環境で規則を回避して使う

## 実装済みの安全対策

- 待受け先を`127.0.0.1`に固定
- 要求全体を65536バイトに制限
- `/delay`を最大10000ミリ秒に制限
- 応答後にクライアントソケットを閉じる
- `Server_Stop`でOnTime予約、ソケット、Winsockを後始末
- Traceの`Authorization`と`Api-Key`ヘッダーを伏せ字化
- Traceの詳細を1000文字で省略
- 外部Shell、PowerShell、`curl`を起動しない

これらは教材上の事故を減らすための対策であり、安全な公開サーバーであることを保証するものではありません。

## トレースに記録してはいけない情報

Traceシートには応答本文が記録されます。学習用の架空データだけを使ってください。特に次の情報を送信しないでください。

- パスワード、APIキー、アクセストークン
- 氏名、住所、電話番号、メールアドレス
- 医療、金融、人事に関する情報
- 社内の非公開URL、サーバー名、障害情報

マスキングは完全ではありません。独自ヘッダー名や本文内に埋め込まれた秘密情報は自動的に検出できません。

## マクロの取り扱い

ダウンロードしたマクロを実行する前に、ソースを読み、内容を理解してください。マクロのセキュリティ設定を無条件に下げたり、組織のセキュリティ製品を無効化したりしないでください。

このリポジトリはShell、PowerShell、`curl`などの外部コマンドを起動しません。ただし、`Server_StartInNewExcel`を利用者が明示的に実行した場合は、簡易サーバー専用の別Excelプロセスを`CreateObject("Excel.Application")`で起動します。組織管理PCでは、利用前に管理者や情報システム部門の方針を確認してください。

## 問題を報告するとき

セキュリティ上の問題を公開Issueへ書くと、悪用方法や機密情報が公開される可能性があります。リポジトリ所有者が指定する非公開の連絡方法がある場合は、そちらを優先してください。

報告には次を含めてください。ただし、実際の秘密情報は削除してください。

- 影響を受けるファイルとプロシージャ
- 再現条件
- 想定される影響
- Officeの32/64ビットとバージョン
- Windowsのバージョン
- 秘密情報を除去したTrace
